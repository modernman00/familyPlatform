import { id, qSel, log } from "../global"

try {
    // id('spouse_div').style.display="none";
id('spouse_div').style.display = "none";

// id("firstName_help").innerHTML = 
} catch (error) {
    log(error.message)
}

